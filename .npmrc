@tindevelopers:registry=https://npm.pkg.github.com
# Auth (a PAT with read:packages) is provided via the user-level ~/.npmrc or a CI secret,
# never committed here:
#   //npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}
